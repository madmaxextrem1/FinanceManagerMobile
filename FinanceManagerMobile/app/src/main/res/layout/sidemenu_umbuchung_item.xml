<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:orientation="horizontal"
    android:id="@+id/SideMenu_Umbuchung_Layout">

        <ImageView
            android:id="@+id/SideMenu_Umbuchung_Image"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="löschen"
            android:layout_marginLeft="0dp"
            android:src="@drawable/ic_add_circle_blue_24dp"
            android:layout_marginTop="5dp"
            android:layout_marginRight="10dp"
            android:backgroundTint="@color/white"
            android:background="@drawable/ic_delete_white_24dp" />

        <TextView
            android:id="@+id/SideMenu_Umbuchung_Title"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="A"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textAlignment="center"
            android:contentDescription="bearbeiten"
            android:foregroundTint="@color/fm_Login_Background_Color"/>

</LinearLayout>